---
import { Image } from "astro:assets";

const { name, logo } = Astro.props;
---
<div class="text-sm grid content-center tooltip hover:scale-[110%] " data-tip={name}>
    <Image
      src={logo}
      height={100}
      alt={name}
    />
  </div>